apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "market-api.fullname" . }}-test-connection"
  labels:
    {{- include "market-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "market-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
